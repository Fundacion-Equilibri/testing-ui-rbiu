name: Playwright Tests

on:
  push:
    branches: ["main", "develop"] # o la rama donde quieras ejecutar
  pull_request:
    branches: ["main", "develop"]

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    env:
      # Reemplaza la siguiente URL con la de tu aplicación desplegada
      BASE_URL: https://mi-app.com

    steps:
      - name: Checkout código
        uses: actions/checkout@v5

      - name: Instalar Node.js
        uses: actions/setup-node@v5
        with:
          node-version: lts/*

      - name: Instalar dependencias
        run: npm ci

      - name: Instalar navegadores de Playwright
        run: npx playwright install --with-deps

      - name: Ejecutar tests
        run: npx playwright test

      - name: Guardar reportes como artefacto
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 20

      - name: Subir informe para GitHub Pages
        if: always()
        uses: actions/upload-pages-artifact@v1
        with:
          path: playwright-report

      - name: Deploy to GitHub Pages
        if: always()
        uses: actions/deploy-pages@v1
